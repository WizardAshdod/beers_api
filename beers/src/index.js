import React from 'react';
import ReactDOM from 'react-dom/client';
import BeerApp from '../src/comps/BeerApp'
import App from './App';

const elem = document.getElementById('root')
const root = ReactDOM.createRoot(elem)

root.render(
  
    <App />

 
);